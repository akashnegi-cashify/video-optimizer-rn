import 'package:flutter_test/flutter_test.dart';

// Actual widget import
import 'package:flutter_trc/qc/modules/stock_in_module/widgets/labeled_text_field.dart';

void main() {
  group('LabeledTextField', () {
    test('LabeledTextField class exists', () {
      expect(LabeledTextField, isNotNull);
    });
  });
}
