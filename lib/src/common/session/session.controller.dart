import 'dart:async';

import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class SessionController {
  static const String UNREGISTERED_USER = '3000';

  static Future<dynamic> handleSessionExpire(NavigatorState state) async {
    final completer = Completer();

    // TODO show ui and complete below returned future

    return completer.future;
  }
}
