import 'package:flutter/material.dart';

class RmsHomeWidget extends StatelessWidget {
  const RmsHomeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("RmsHomeWidget"));
  }
}
