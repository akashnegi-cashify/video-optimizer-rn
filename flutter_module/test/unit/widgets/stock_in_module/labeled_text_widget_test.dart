import 'package:flutter_test/flutter_test.dart';

// Actual widget import
import 'package:flutter_trc/qc/modules/stock_in_module/widgets/labeled_text.dart';

void main() {
  group('LabeledText', () {
    test('LabeledText class exists', () {
      expect(LabeledText, isNotNull);
    });
  });
}
